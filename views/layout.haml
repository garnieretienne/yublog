%html
  %head
    %meta{'charset' => 'UTF-8'}
    %title= "#{settings.name}: #{@title}"
    %link{:rel => 'stylesheet', :href => '/css/design.css'}
    %link{:rel => 'stylesheet', :href => '/css/monokai.css'}
  %body
    %a{:href => '/', :id => 'link'}
      #header
        %h1= settings.name
        %h2= settings.description
    = yield
  - @javascripts.each do |js|
    = "<script src='/js/#{js}.js' type='text/javascript'></script>"

